# US-002 — Claude Code hooks 一键接入

## 1. Automated Verification (Code-Based)

- [ ] `node --check` 全部改动文件
- [ ] `node tests/claude-hook-test.js` 与 `node tests/claude-installer-test.js` 通过

## 2. Model-Based Verification

- [ ] `hooks/claude-status-hook.js` 是独立可执行脚本（宿主未运行时 Claude Code 也在调它）：只用 Node 内建，不 require 仓内其它模块以外的依赖；从 stdin 读完整 JSON；按 `hook_event_name` 走 PROTOCOL.md 映射；未知事件/解析失败/写盘失败一律 `process.exit(0)` 且不向 stdout 写会干扰 Claude Code 的内容
- [ ] tty 与 pid 采集：进程可得时写实值（`process.stdin` 非 tty 时经 `ps` 查父链或置 null），拿不到写 null 不猜
- [ ] 安装器 merge 语义（2026-09-10 监工裁决澄清）：用户 hooks 条目**语义等价 + 键序保持**地保留（JSON 重排版不算违规）；若原文件本就是 `JSON.stringify(...,null,2)+\n` 风格则卸载后**逐字节一致**（保底断言）；本插件条目带可识别标记（matcher/command 含 `pet-agent-status`）；重复安装不重复追加；`uninstall` 只删含标记条目
- [ ] 备份「已存在不覆盖首份」：第二次 install 不得刷新 `.bak-pet-agent-status`，断言两次 install 后备份内容 === 首次安装前原文
- [ ] 插件装在**含空格路径**下 hook 仍可用：hookCommand 对路径做引号安全，测试用含空格临时目录 spawn hook 断言 rc=0 且状态文件生成
- [ ] 备份：写前复制原文件为 `.bak-pet-agent-status`（已存在不覆盖首份）；原文件不存在时创建合法最小结构
- [ ] hooks 事件注册覆盖：SessionStart / UserPromptSubmit / PreToolUse / PostToolUse / Notification / Stop / SessionEnd

## 3. 独立复验

- [ ] verify agent 自写脚本：构造含用户自有 hook 的 settings.json 夹具（**至少含一种非 2 空格风格**：tab/紧凑/无末尾换行，防与实现同源自证）→ install → uninstall → 断言语义等价 + 键序保持；另用 stringify(,,2) 风格夹具断言保底逐字节一致
- [ ] 挑 2 个 fixture 事件（Notification、Stop）spawn 脚本喂 stdin，cat 状态文件断言 state=waiting/done

## 4. Full Test Procedure

1. 跑两个仓内测试
2. 第 2 节逐条读码
3. 第 3 节独立脚本（/tmp，跑完删）

## 5. What a Passing Report Looks Like

```
VERIFICATION REPORT - US-002
node --check:            PASS
hook 事件映射测试:       PASS（7 事件逐一）
installer 幂等/卸载:     PASS（语义等价+键序+保底逐字节+备份首份不覆盖）
静默失败保护:            PASS（坏 JSON 喂 stdin 退出码 0）
Overall: PASS
```

## 6. Common Failure Cases & Handling

- hook 脚本非零退出或向 stdout 打印垃圾（会干扰 Claude Code）→ FAIL
- 卸载后用户自有条目丢失/顺序被改 → FAIL
- 测试直调模块函数而非 spawn 喂 stdin → 自证，criteria 要求真实通道 → FAIL
- fixtures 与官方事件字段名不符（session_id/cwd/hook_event_name）→ FAIL
