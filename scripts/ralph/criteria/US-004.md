# US-004 — panel 会话列表与空态 UI

## 1. Automated Verification (Code-Based)

- [ ] `node --check` 通过（panel 逻辑组织成可校验的 JS）
- [ ] `node tests/panel-dom-test.js` 通过

## 2. Model-Based Verification（设计红线：逐项对照 DESIGN.md + docs/design/*.png）

- [ ] token 色值逐个与 DESIGN.md 表一致（Ink #2A2E39 / #3D7EFF / #F2994A / #27AE60 / #EB5757 / #9AA0AC / 行底 rgba(255,255,255,.06) / waiting 行橙 13%+1.5px 描边）
- [ ] 行结构与尺寸：徽标 26×26 圆角 8、角标 13×13、项目名 13、副行 11、状态点 8、圆角 12
- [ ] 徽标资源：`assets/claude.svg`/`assets/openai.svg` 存在且为 Simple Icons 官方 path（viewBox 0 0 24 24）；本地引用，无远程 URL
- [ ] 空态三要素 + 接入按钮态机（未接入→蓝主按钮；已接入无会话→已接入 ✓ + 移除钩子；Codex 显『即将支持』灰字且无 click handler）
- [ ] 无外部依赖：panel.html `grep -E 'https?://'` 零命中（注释除外）；无框架
- [ ] 文案全部经 i18n 取词

## 3. jsdom DOM Verification（本仓库的 UI 验收通道，代替 CDP）

- [ ] 喂 5 行快照（含 waiting×1 乱序、running×2、done、error）：断言渲染行数、waiting 首位且有描边 class、各行副行颜色 class 正确、汇总胶囊「2 运行中」
- [ ] running 行 timeText 呈 mm:ss；done 行呈相对时间
- [ ] 空快照 + 未接入 → 空态 DOM；点『一键接入』按钮 → mock pet.events 收到 agent-status:install-claude 意图（按钮只发意图，逻辑在 tool 侧）
- [ ] 点会话行 → mock pet.events 收到 agent-status:jump{sessionId}
- [ ] 快照带 jumpError 的行渲染行内错误条（Danger 色）

## 4. Full Test Procedure

1. `node tests/panel-dom-test.js`
2. 第 2 节逐项对照 DESIGN.md（色值用 grep 比对十六进制）
3. verify agent /tmp 自写一份最小 jsdom 断言（排序 + waiting 描边）交叉复验

## 5. What a Passing Report Looks Like

```
VERIFICATION REPORT - US-004
node --check:        PASS
panel DOM 测试:      PASS（行数/排序/颜色/空态/意图）
设计红线逐项:        PASS（token/尺寸/徽标/文案 i18n）
无外部依赖:          PASS（grep 零命中）
Overall: PASS
```

## 6. Common Failure Cases & Handling

- 色值/尺寸与 DESIGN.md 有出入 → 设计红线 → FAIL
- 徽标用字母占位而非官方 SVG → FAIL
- Codex 灰字挂了假 click handler → FAIL（禁假入口）
- panel 里直接做业务判断（排序/推导）→ 行结构应来自 US-003 快照，panel 只渲染 → FAIL
