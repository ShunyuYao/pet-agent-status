# US-006 — Codex CLI hooks 接入

## 0. 前置门（先查再动）

- [ ] `fixtures/codex-hooks-facts.md` 存在。**不存在 → 本 story BLOCKED**：progress.txt 写明，跳过不实现。存在才继续。

## 1. Automated Verification (Code-Based)

- [ ] `node --check` 全部改动文件
- [ ] `node tests/codex-hook-test.js` 通过（夹具必须来自 facts 附带的实录 payload，不许手造）

## 2. Model-Based Verification

- [ ] 实现与 facts 文件逐条对得上：配置路径、事件名、payload 字段名、注册方式（含 facts 里记录的可信 hash 机制若有）
- [ ] installer 标准同 US-002（备份/幂等/仅摘除自有条目卸载），配置路径经 `PET_AS_CODEX_HOOKS` 可覆盖
- [ ] 状态文件 `agent:'codex'`；facts 里有 threadId 来源则写入（UUID 校验后），没有则不造
- [ ] PROTOCOL.md「Codex 映射表」按实测补全：只新增行，Claude Code 已有行零改动（git diff 核对）
- [ ] 零特判贯通：aggregate/panel/jump 对 codex 行无新增分支（徽标/form 由既有 agent 字段驱动）——grep 'codex' 在 lib/aggregate.js、panel 里只应命中数据驱动映射表，不应命中流程分支

## 3. 独立复验

- [ ] /tmp 自写脚本：用 facts 实录 payload spawn codex hook 喂 stdin，cat 状态文件断言 agent/state/threadId；再喂进 aggregate 断言与 claude 行同构输出

## 4. Full Test Procedure

1. 前置门 → 仓内测试 → 读码逐条对 facts → 独立脚本

## 5. What a Passing Report Looks Like

```
VERIFICATION REPORT - US-006
前置门 facts:        PRESENT
node --check:        PASS
实测事实一致性:      PASS（逐条）
零特判贯通:          PASS
PROTOCOL 只增不改:   PASS（git diff）
Overall: PASS
```

## 6. Common Failure Cases & Handling

- facts 缺失仍实现了 → 违反先复现硬规矩 → FAIL 并回退
- 事件名/字段与 facts 不符（凭训练记忆写）→ FAIL
- 为 codex 在 panel/aggregate 加流程特判 → 协议先行被破坏 → FAIL
