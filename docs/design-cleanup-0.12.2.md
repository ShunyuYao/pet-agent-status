# 0.12.2 设计整理与图标尺寸

最新视觉权威为 [Figma](https://www.figma.com/design/UJimpWGl2hGkrbzxIVCAK5?node-id=0-1)。2026-09-12 用户确认底栏缩小后的效果，并授权清理旧稿、同步实现和发版。

## 当前设计入口

- `0:1`「01 · 当前面板」：有会话/无会话与已安装/未安装 App 的四种组合，节点分别为 `40:41`、`40:81`、`44:2`、`44:38`。
- `92:2`「02 · 组件与交互」：常驻徽标、三/两 App 启动器、状态图例、气泡及当前行为规格。

删除旧文字底栏面板、旧空态、“Codex 即将支持”及旧徽标消失/宿主扩展提案；保留当前有用的组件和交互。四种面板均同步设置与关闭图标，未安装 App 的列表示例以真实产品图标替代灰色占位。长说明改为自动高度，修复图例文字溢出。最终两页顶层区域无重叠，保留的说明文字均在父容器内。

绿点语义同时对齐已合入主分支的 `84a1712`：有尚未查看的完成会话时点亮，按列表顺序逐条跳转和收起；全部查看后消失，无完成项时正常启动 App。Figma 使用 `pending-done-dot` 命名，主面板示例将绿点放在有完成会话的 Codex 图标上。

## 实现与视觉核对

底栏显示框由32×32缩为28×28，与会话图标一致。点击区域40×40、间距14、分隔线和状态点位置不变。沿用原始 App PNG：裁切框28×28、图片35×35、偏移-3.5，四周内缩6。未改宿主、权限、状态协议或图标源文件。

先更新在线设计、规格与既有真实宿主几何断言，原实现得到182通过/21失败；更新CSS后203项全部通过。检查三种App的图像原始字节、实际解码、裁切尺寸、点击区域、居中与空态。

[并排对照图](design/cleanup-0.12.2/comparison.png)左为Figma、右为真实隐藏宿主，统一显示为320×420。宿主多一条Codex App样本用于验证形态角标；状态文案、汇总和聚焦标记来自实际采集结果。最新设计缓存已更新到 `docs/design/panel-sessions.png`、`panel-empty.png` 与 `legend-notes.png`。

## 测试连接时序

整合前的开窗测试记录了2510ms和703ms的总耗时，首次DOM检查时会话已存在；整合后的另一轮为63ms，但捕获到新面板的Electron sandbox初始化错误 `binding.startupData=null`，门禁仍按失败处理。没有过滤这些错误或放宽700ms门槛。

共享隐藏宿主helper现在对每个新面板等待生产代码发出的 `agent-status:panel-ready` 宿主日志，再连接CDP。每次宿主启动重置读取位置，每个新面板消费下一条就绪事件；不修改生产状态、不以固定延时或模拟事件代替就绪。相同开关路径复核133ms、无renderer异常。完整门禁仍要求21个离线套件和8条真实隐藏宿主E2E全部通过，并在合并主分支后重跑。

E2E覆盖：figma-layout、ui-parity、app-launcher、launcher-completion、dismiss、waiting-accuracy、panel-scroll、panel-open。证据中的失败记录保留在本轮发布审计目录；隐藏测试不声称覆盖原生鼠标穿透或日常外部App的真实激活。

首轮工作分支整轮 `npm run test:ui` 通过：21个离线套件、8条隐藏宿主E2E，figma-layout 203/203，panel-open 129ms。

首次主分支复核发现：调试连接中途断开后，同一个面板被重复要求发送只触发一次的就绪事件，导致测试超时。新增真实关闭WebSocket后重连的回归路径，修复前稳定超时；helper改为按面板target记录已就绪状态、在宿主重启时清空，修复后同一面板仍显示3个App，figma-layout 204/204。没有改变产品行为或放宽门槛。

重连修复后工作分支再次整轮 `npm run test:ui` 通过：21个离线套件、8条隐藏宿主E2E，figma-layout 204/204，panel-open 133ms，renderer无异常。
