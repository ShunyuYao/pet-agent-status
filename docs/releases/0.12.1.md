# 0.12.1

- 面板打开后立即显示已采集的会话状态，减少等待下一轮轮询造成的空白。本机隔离宿主实测从约 1.87 秒降至约 0.1 秒；实际耗时随设备和启动状态变化。
- 按最新 Figma 修正会话图标、App 角标、底部启动器、字体和空态布局，保留真实产品图标。
- 统一离线与隐藏宿主界面回归，覆盖开窗、会话收起、批准状态、设置和滚动。

本版不改变会话采集协议和权限，也不包含尚未稳定复现的原生焦点开合问题修复。

发布包由公开 CI 从提交生成。使用 `git archive` 固定文件顺序、时间与权限元数据，同一提交的本地包和 CI 包须通过 SHA-256 一致性核验。
